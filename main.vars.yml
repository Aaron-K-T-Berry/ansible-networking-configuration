---
# Friendly name to be used when tagging demo resources
project_name: "ansible-networking-demo"

# Region you want to deploy resources too
aws_region: ap-southeast-2

# Ip address that will be used to connect to AWS resources
# Use XXXX.com to find your current ip address on the internet
local_ip_address: 124.171.65.12

# Replace with your desired key name in AWS
key_name: "ansible-networking-demo"
# Path to your generated private SSH key
local_key_path: "../keys/aws_id_ed25519.pub"
